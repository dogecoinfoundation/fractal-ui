import { SetupCard } from "@/components/setup/setup-card";

export const Connection = () => {
  return (
    <SetupCard
      cardDescription="Connection"
      cardContent={
        <>
          <h2>Connection</h2>
          <p>etc</p>
        </>
      }
    />
  );
};
